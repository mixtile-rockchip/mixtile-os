# shellcheck shell=bash
# GNOME. The same GNOME on both distributions, so the bulk of the list is
# shared and only the metapackage names differ per release.
export FLAVOR_PACKAGE_LISTS="base desktop"
